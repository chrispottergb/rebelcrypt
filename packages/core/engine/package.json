{
  "name": "@music-ai/engine",
  "version": "0.1.0",
  "description": "Autogenes Workflow Engine — DAG-based workflow execution with multi-tenant isolation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/uuid": "^9.0.0",
    "@types/node": "^20.0.0"
  }
}
