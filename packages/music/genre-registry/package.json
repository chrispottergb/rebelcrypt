{
  "name": "@music-ai/genre-registry",
  "version": "0.1.0",
  "description": "Global music genre taxonomy — 200+ genres with hierarchy, regions, eras",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "jest" },
  "devDependencies": { "typescript": "^5.4.0" }
}
